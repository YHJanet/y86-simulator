interfaces/types.py
定义了各板块需用的核心数据结构类型

execution/execution_unit.py
指令执行单元，负责指令解码和算术运算

control/control_unit.py
控制流单元，负责程序执行流程和状态管理

memory/memory_unit.py
内存管理单元，负责内存读写和栈操作

main.py
整合各板块

缓存机制：
内存由python的map类实现；
缓存则是组+行的传统形式，采用写穿透+写不分配策略（即不存在dirty位）。
在访问内存前需要判断地址是否合法，如果不合法，要设置STAT码为3。
优势：
内存按需分配，无需预设大小，
而且对于地址稀疏的实验环境能有效提高效率，因为数组存取比map更快。
此外，缓存统计功能可监控命中率，便于性能分析。
